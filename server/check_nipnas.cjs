const { PrismaClient } = require('@prisma/client')require('dotenv').config()const prisma = new PrismaClient()async function main() {  const countWithNipnas = await prisma.sosData.count({    where: {      nipnas: {        not: null      }    }  })  console.log(`Records with nipnas (not null): ${countWithNipnas}`)  const samples = await prisma.sosData.findMany({    where: { nipnas: { not: null } },    take: 5,    select: { nipnas: true, poName: true }  })  console.log(samples)}main()  .catch(e => {    console.error(e)    process.exit(1)  })  .finally(async () => {    await prisma.$disconnect()  })
