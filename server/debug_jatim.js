import prisma from './src/lib/prisma.js'async function debugJatimBarat() {  try {    // Find all 'JATIM BARAT' records    const records = await prisma.sosData.findMany({      where: {        OR: [          { custWitel: { contains: 'JATIM BARAT' } },          { serviceWitel: { contains: 'JATIM BARAT' } },          { custCity: { contains: 'JATIM BARAT' } }        ]      },      select: {        id: true,        custWitel: true,        serviceWitel: true,        custCity: true      }    })    console.log(`Found ${records.length} records related to JATIM BARAT`)    // Group by City to see what cities are lumped under Jatim Barat    const cityCounts = {}    records.forEach(r => {        const city = r.custCity || 'NULL'        cityCounts[city] = (cityCounts[city] || 0) + 1    })    console.log('--- Distribution by Cust City ---')    Object.entries(cityCounts)        .sort(([,a], [,b]) => b - a)        .forEach(([city, count]) => console.log(`"${city}": ${count}`))  } catch (error) {    console.error(error)  } finally {    await prisma.$disconnect()  }}debugJatimBarat()
